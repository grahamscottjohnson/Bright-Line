/*
 * Action Types
 */

/*
 * Action Creators
 */

/*
 * Initial State
 */

const initialState = []

/*
 * Reducer
 */

const blocks = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default blocks
