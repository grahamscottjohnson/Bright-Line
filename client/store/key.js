/*
 * Action Types
 */

/*
 * Action Creators
 */

/*
 * Initial State
 */

const initialState = {x: 10, y: 0}

/*
 * Reducer
 */

const i = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default i
